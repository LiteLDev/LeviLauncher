# UI / UX 修复记录

依据：`frontend/.artifacts/ui-ux-audit-2026-09-09/UI-UX-AUDIT.md` 及三个详细分支报告。审计基线为 `0d522a6`。

本次保持现有导航、页面分区、列表和弹窗结构，逐项修复报告中的 31 条问题。按追加要求，界面统一使用纯色，移除渐变文字、背景、边框、进度条、骨架闪光及滚动渐隐；自定义选色使用纯色色样与数值输入。

## 修复对应表

| ID | 修复结果 |
| --- | --- |
| PRI-01 | 实例未保存检测包含启动参数和环境变量，保存前离开会明确确认。 |
| PRI-02 | 实例设置和全局设置保留数字历史步长，保存后前进不再变为相对路径。 |
| PRI-03 | 下载列表区分加载、失败、空数据和筛选无匹配；首次失败可重试，刷新失败保留缓存与提示。 |
| PRI-04 | 首页和实例列表提供首次下载、本地安装入口，失败可重试，筛选可清除。 |
| PRI-05 | 校验阶段保持活跃状态，显示校验文案，终态才允许移除记录。 |
| PRI-06 | 安装失败保留错误摘要、详情、复制和重试；旧安装回滚完成后才允许重试。 |
| PRI-07 | 未应用的新目录需要明确确认，沿用当前目录有准确说明，不再静默完成。 |
| PRI-08 | 面包屑通过统一路由映射本地化，详情显示真实名称，历史提示与之保持一致。 |
| PRI-09 | 搜索、本地安装、取消下载、移除记录等操作采用准确图标和文案；搜索清除与筛选清除分开命名。 |
| PRI-10 | 安装开关、下载操作和实例设置按钮补全可访问名称。 |
| PRI-11 | 长实例名称提供完整文本提示，键盘聚焦同样可查看。 |
| PRI-12 | 安装成功可选择新实例并前往首页启动入口。 |
| C01 | 世界编辑器派生 dirty 状态，使用路由框架 `useBlocker` 保护应用内导航及历史返回，刷新另行确认；保存使用当前草稿快照并留在编辑器。 |
| C02 | 选择按实例、玩家隔离；确认展示范围和对象名称；迟到请求、账户自动识别及旧删除收尾不能覆盖新作用域；筛选隐藏的选择有数量提示。 |
| C03 | 五类内容删除按成功、失败数量反馈，识别后端 `ERR_*` 返回值；失败对象继续选中，错误详情保留并可重试。 |
| C04 | 数据减少或筛选收缩时立即将页码和切片收敛到合法范围。 |
| C05 | 操作区支持 `focus-within` 显示；内容选择和图标按钮有本地化名称与可见焦点。 |
| C06 | 截图元数据先呈现，图片通过本地 URL 和浏览器懒加载逐项显示，取消全量 Base64 读取。 |
| C07 | 截图区分未选择玩家、无截图、列表读取失败和单图失败，分别提供返回或重试。 |
| C08 | 只读卡片取消可点击手势，选择模式与实际操作保留对应手势。 |
| P2-01 | 保留品牌白字，实心主操作色按对比度加深；悬停保持对比度，深色焦点使用独立可见颜色。 |
| P2-02 | CurseForge / LIP 分享复制公开项目链接并显示结果；没有可靠公开地址时不显示分享动作。 |
| P2-03 | 在线目录详情入口使用语义链接，支持 Tab 与 Enter。 |
| P2-04 | 主题色块具有颜色名、色值、选中语义、Tooltip 和键盘操作。 |
| P2-05 | CurseForge 元数据、描述和文件分别加载与重试，空描述有明确终态。 |
| P2-06 | 路径保存与恢复默认共用结果处理，失败保留错误与弹窗，成功后才更新界面和关闭确认。 |
| P2-07 | 更新失败明确显示“关闭更新窗口”及恢复说明。 |
| P2-08 | DLL 名称为空时显示必填提示并禁用确认，输入有效名称后恢复。 |
| P2-09 | 说明、错误、README、路径和包 ID 支持文本选择与复制。 |
| P3-10 | 模组菜单、文件安装和外链图标补齐名称与提示，文件动作包含版本信息。 |
| P3-11 | 本地未安装、筛选无匹配、在线目录加载失败采用不同文案和恢复入口。 |

附带完成：迁移及覆盖导入按钮采用动作名称；下载工具栏适配小窗口，页脚数量不再断行；信息卡片与导航增加纯色底的不透明度，提高自定义背景上的可读性。

## 验证

前端回归覆盖位于：

- `frontend/e2e/ui-audit-primary.spec.ts`：未保存字段、数字历史前进、首次错误恢复、校验、安装失败与成功下一步、引导和长实例名。
- `frontend/e2e/ui-audit-content.spec.ts`：世界编辑与历史保护、玩家作用域、部分删除失败、分页、截图加载恢复。
- `frontend/e2e/ui-audit-mods.spec.ts`：独立请求、分享、键盘链接、路径保存、选色、DLL 校验及更新文案。
- `frontend/e2e/ui-audit-shared.spec.ts`：预设与极端自定义色对比度、焦点、中文面包屑、文本复制和无渐变要求。
- 现有 `accessibility.spec.ts` 已取消品牌白字区域的对比度排除；现有交互回归同步更新准确动作名称。

最终验证结果：

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过：TypeScript、生产构建和全部体积预算检查通过。总产物 8,216,041 / 9,500,000 字节，JavaScript 2,725,258 / 3,800,000 字节。 |
| `npm run test:e2e -- --workers=3 --output=.artifacts/playwright-results-verified` | **69 / 69 通过**，42.6 秒。 |
| `node node_modules/@playwright/test/cli.js test --config .artifacts/ui-ux-fixes/visual.config.ts` | **50 / 50 通过**，49.7 秒；保存截图，并检查各路由可见元素及伪元素的背景、遮罩和边框无渐变。 |
| 无渐变源码检查 | 通过：应用源码中无 CSS/Tailwind 渐变或 SVG 渐变填充定义。 |
| `git diff --check` | 通过。 |

窗口检查后另对浅色首页、下载页、首次引导、浅深设置页和自定义背景截图进行了人工视觉复核。无障碍自动化只覆盖对应测试场景，不作为全应用 WCAG 合规声明。

## 证据范围

浏览器验证使用隔离 Chromium 和 Wails 接口测试替身，不读取或修改用户真实存档，不实际安装、删除或更新游戏。窗口截图覆盖全部 23 个路由的浅色 960×600、深色 1440×900，并另外检查个性化页面的自定义背景与两种窗口尺寸。

本次证据证明前端状态、交互、请求参数和呈现行为；不等同于原生 Wails 窗口与真实文件系统、账号服务和更新器的全链路验收。截图及过程日志保存在被 Git 忽略的 `frontend/.artifacts/ui-ux-fixes/` 下。
